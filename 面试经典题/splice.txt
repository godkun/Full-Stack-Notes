var a=[1,2,3,4,5];
a.splice(1,3,5,2,1);

上面是从数组下标为1的时候开始删除，删除包括a[1]
然后连续删除3个元素，还剩a[1,5]

splice后面参数，是表示添加元素，在新数组数组下标1的位置开始，添加元素
5 2 1 也就是   [1,5,2,1,5]

如果写成
a.splice(0,3,5,2,1);
则表示删除后的元素为[2,1]，然后在下标0的位置开始添加添加元素
也就是[5,2,1,4,5]