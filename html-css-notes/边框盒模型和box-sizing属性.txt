内容盒模型：规定width和height的属性设置为给定内容区域的尺寸

上面也叫做标准CSS盒模型



box-sizing属性：
默认值是content-box，指定标准盒模型

如果使用border-box，则浏览器将会为那个元素应用IE的盒模型，也叫边框盒模型。
即width和height属性包含边框和内边距。

边框盒模型的优势：
以百分比形式为元素设置总体尺寸，又想以像素单位指定边框和内边距时，非常有用

CCS3中可以使用calc()来计算CSS的值


