事件传播包括事件冒泡，事件处理程序调用，事件捕获。

顺序是：
如果注册事件处理程序里的第三个参数是true，则事件处理程序被注册为
捕获事件处理程序，它会在事件传播的第一个阶段调用。

随后第二个阶段，对目标对象本身的事件处理程序调用
第三个阶段，进行事件冒泡

其中事件捕获的目的是：
提供了在事件没有送到目标之前查看它们的机会，事件捕获可以用于程序调试。
事件捕获常用与处理鼠标拖放，因为要处理拖放事件的位置不能是这个元素内部
的子元素。