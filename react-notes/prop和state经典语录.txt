1：prop是组件的对外接口， state是组件的内部状态

对外用prop，对内用state


2：当prop的类型不是字符串类型时，在JSX中必须用花括号{}把prop值包住
所以style的值有两层花括号，外层花括号代表是JSX的语法，内层花括号代表
这是一个对象常量。


3：prop支持的类型可是JS中任何一种数据类型

4：组件的state必须是一个JS对象





