1：返回一系列值，也是生成了一系列值

2：返回一个遍历器对象

3：特征：function*
	 内部使用yield语句

4：调用函数后，函数不会执行，返回的也不算函数运行结果，而是指向内部状态的指针对象。

5：必须调用next方法，函数是分段执行的，yield语句是暂停执行的标记，next方法是回复执行

6：用途 : 处理异步操作，改写回调函数。

7：genertor可以看作是一个数据结构，确切是看成数组结构




