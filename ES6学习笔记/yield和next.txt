1：next方法的参数表示上一条yield语句的返回值，所以第一次使用next方法时
不能带有参数。

2：yield语句本身没有返回值，也就是总是返回undefined

3：后面的表达式不会立即求值，只会在next方法将指针移到这一句时才求值。

4：yield如果在一个表达式内，必须放到圆括号内

5：yield语句是同步运行，不是异步运行

6：






